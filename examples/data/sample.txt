QueryNest 是一个多模态文档 RAG 系统。

混合检索（Hybrid Retrieval）包含以下主要召回路径：
1. Dense：向量检索（LightRAG local 模式）
2. Keyword：纯 Python BM25 关键词检索
3. Graph：知识图谱检索（LightRAG global 模式）

多路召回结果通过 RRF（Reciprocal Rank Fusion）融合，随后去重、可选重排，
最终构造上下文，供 LLM 生成带可引用来源的回答。